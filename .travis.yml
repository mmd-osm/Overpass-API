language: cpp

compiler:
  - gcc

before_install:
  - sudo apt-get install expat libexpat1-dev 
  
before_script:  
  - pushd src/
  - autoscan
  - aclocal
  - autoheader
  - libtoolize
  - automake --add-missing
  - autoconf
  - popd
  - mkdir -p build
  - cd build
  - ../src/configure CXXFLAGS="-O3" --prefix="."

script:  
  - make

branches:
  only:
    - travis_test

os:
  - linux
